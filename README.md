### Run Windows:
    `./mqtt_run.bat`

### Run Linux:
    `./mqtt_run.sh`

### MQTT SERVER:
    `pipenv install`
    `pipenv run python mqtt_broker.py`

### NGROK:
    `ngrok start --config ngrok.yml --all`